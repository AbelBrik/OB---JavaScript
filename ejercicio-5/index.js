const height = 170;
const floatHeight = 1.7;
const weight = 60.3;
const heightCeil = Math.ceil(floatHeight);
const weightFloor = Math.floor(weight);
const maxNumber = Number.MAX_VALUE === Number.MAX_VALUE++;